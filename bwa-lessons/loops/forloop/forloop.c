#include <stdio.h>

/**
 * main - main entry
 * Description: For loop conditional statement. Begins with:
 * for (initial value, condition, incrementation or decrementation)
 {
 statement here
 }
 * Return: Always 0
 */

int main()
{
	int num = 0;

	for (num = 0; num <=10; num++)
	{
		printf("%d\n", num);
	}

	return 0;
}
